id = "cargo-dist"
name = "cargo-dist"
repository = "https://github.com/axodotdev/cargo-dist"
category = "release"
crate = "cargo-dist"

remarks = """
Builds and packages cross-platform binary artifacts and installers (shell/PowerShell
installers, archives, npm/Homebrew formulae) and wires up the CI to produce them
on a tagged release.

Use it when you ship prebuilt binaries rather than just publishing a crate. Note
the project has been renamed to `dist`; newer releases and docs live under that
name. It handles distribution, not version bumping, so pair it with
`cargo-release` or `release-plz`.
"""

alternatives = ["release-plz", "cargo-release"]

# ── Auto-generated by the metrics bot: do not edit by hand ──
[metrics]
stars = 2103
forks = 147
open_issues = 332
last_commit = "2026-08-20"
archived = false
license = "Apache-2.0"
updated_at = "2026-08-21T03:45:23.786651040+00:00"
stale = false

[metrics.crate]
name = "cargo-dist"
downloads_total = 171738
downloads_recent = 21944
latest_version = "0.32.0"
latest_release = "2026-05-22"
msrv = "1.74"
license = "MIT OR Apache-2.0"
owners = ["Aria Desires", "ashley williams", "Misty De Méo", "axoadmin", "eng"]
