id = "probe-rs"
name = "probe-rs"
repository = "https://github.com/probe-rs/probe-rs"
category = "embedded"
# The crates.io `probe-rs` crate (tracked for metrics) is a library; the CLI
# ships as `probe-rs-tools`, so keep it out of derived `cargo install` lines.
installable = false
crate = "probe-rs"

remarks = """
A toolkit for debugging and flashing embedded Rust on real hardware. It talks
to debug probes (CMSIS-DAP, J-Link, ST-Link) to flash firmware, reset targets,
and run an in-app log/RTT console, and ships `cargo embed` plus a GDB/DAP
server for editor debugging. It's the modern replacement for OpenOCD-based
workflows in the embedded Rust ecosystem.

This is not a cross-compilation tool: you still need to install the appropriate
Rust target (for example with `rustup target add`) and configure the target and
linker for your microcontroller. Reach for probe-rs once you need to put that
firmware onto a device or inspect it while it runs. It's irrelevant for hosted
(non-embedded) targets, and very capable probes with vendor-specific features
may still need their proprietary tooling.
"""

alternatives = ["OpenOCD", "cargo-flash", "cargo-embed"]
recommended = true

# ── Auto-generated by the metrics bot: do not edit by hand ──
[metrics]
stars = 2923
forks = 629
open_issues = 350
last_commit = "2026-08-31"
archived = false
license = "Apache-2.0"
updated_at = "2026-08-31T09:13:02.129663019+00:00"
stale = false

[metrics.crate]
name = "probe-rs"
downloads_total = 655694
downloads_recent = 167144
latest_version = "0.32.0"
latest_release = "2026-07-22"
msrv = "1.89"
license = "MIT OR Apache-2.0"
owners = ["Noah Hüsser", "Dominik Boehi", "crate-owners"]
