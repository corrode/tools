id = "taplo"
name = "taplo"
repository = "https://github.com/tamasfe/taplo"
category = "formatting"
crate = "taplo-cli"

remarks = """
A TOML toolkit providing a formatter, linter, and language server for TOML files
such as `Cargo.toml` and `rustfmt.toml`.

Install with the `taplo-cli` crate and run `taplo fmt` or `taplo lint`; use
`taplo fmt --check` in CI. Reach for it to keep config files tidy and validated
against schemas. It handles TOML only, so pair it with `rustfmt` for Rust source.
"""

alternatives = ["rustfmt"]

# ── Auto-generated by the metrics bot: do not edit by hand ──
[metrics]
stars = 2383
forks = 187
open_issues = 237
last_commit = "2026-07-28"
archived = false
license = "MIT"
updated_at = "2026-09-05T07:18:23.508644141+00:00"
stale = false

[metrics.crate]
name = "taplo-cli"
downloads_total = 2169632
downloads_recent = 188810
latest_version = "0.10.0"
latest_release = "2025-05-23"
msrv = "1.74"
license = "MIT"
owners = ["Ferenc Tamás", "JounQin"]
