id = "cargo-deny"
name = "cargo-deny"
repository = "https://github.com/EmbarkStudios/cargo-deny"
category = "security"
crate = "cargo-deny"

remarks = """
A policy linter for your dependency graph, checking security advisories,
license compliance, banned or duplicate crates, and allowed source registries.

Configure rules in `deny.toml` and run `cargo deny check` in CI. Broader than
`cargo-audit`, which covers only advisories. Reach for it when you need to
enforce organizational policy across all four areas in one pass.
"""

alternatives = ["cargo-audit", "cargo-vet"]
recommended = true

# ── Auto-generated by the metrics bot: do not edit by hand ──
[metrics]
stars = 2412
forks = 128
open_issues = 87
last_commit = "2026-08-26"
archived = false
license = "Apache-2.0"
updated_at = "2026-09-02T07:30:47.609324436+00:00"
stale = false

[metrics.crate]
name = "cargo-deny"
downloads_total = 5411501
downloads_recent = 1492163
latest_version = "0.20.2"
latest_release = "2026-07-09"
msrv = "1.88.0"
license = "MIT OR Apache-2.0"
owners = ["embark-studios"]
