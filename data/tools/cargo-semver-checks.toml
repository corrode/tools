id = "cargo-semver-checks"
name = "cargo-semver-checks"
repository = "https://github.com/obi1kenobi/cargo-semver-checks"
category = "linting"
crate = "cargo-semver-checks"

remarks = """
Lints a crate's public API for SemVer-violating changes between releases,
catching breaking changes that would require a major version bump.

Run `cargo semver-checks` before publishing to verify your version bump matches
the actual API surface. It analyzes the API for breakage but cannot detect
behavioral or semantic changes, so it complements rather than replaces tests.
"""

alternatives = ["clippy"]

# ── Auto-generated by the metrics bot — do not edit by hand ──
[metrics]
stars = 1631
forks = 131
open_issues = 174
last_commit = "2026-05-31"
archived = false
license = "Apache-2.0"
updated_at = "2026-06-04T10:13:36.847043+00:00"
stale = false

[metrics.crate]
name = "cargo-semver-checks"
downloads_total = 435681
downloads_recent = 74897
latest_version = "0.48.0"
latest_release = "2026-05-31"
license = "Apache-2.0 OR MIT"
owners = ["Predrag Gruevski"]
