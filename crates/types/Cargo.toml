[package]
name = "types"
version.workspace = true
edition.workspace = true
publish.workspace = true

[dependencies]
anyhow = { workspace = true }
chrono = { workspace = true }
serde = { workspace = true }
toml = { workspace = true }
utoipa = { workspace = true, optional = true }

[features]
# Enables `utoipa::ToSchema` derives so the same types power the OpenAPI spec.
openapi = ["dep:utoipa"]

[lints]
workspace = true
